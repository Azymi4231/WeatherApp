import React from 'react';
import { Header } from '../features';

const Page = () => {
  return (
    <div className="flex flex-grow flex-col p-5">
      <Header />
    </div>
  );
};

export default Page;
